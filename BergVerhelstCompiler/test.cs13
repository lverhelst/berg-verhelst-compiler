bool readBool(void){;}
int readNum(void){;}
void writeBool(bool x){;}
void writeNum(int x){;}

--always true
bool readBool(void) {
	int arr[20];
	int x;
	arr[1 + 2] := 2;
	x := arr[3];
	return true;
}

--outputs a random preset number so its not really random at all
int readNum(int abc, ref int x) {
	return 42;
}

--used to test the case statement
void testcase(void) {
	int x;
	branch (x)
		case 0: call();
		case 1:  ;
		default: ;
	end;
}

--main program to run
int main(void) {
	int x;
	int y;
	x := 0;
	y := -5 * 1/2;
	y := 5 mod 6;


	--test the loop function
	loop
		--test the if function
		if((x < y) && (x > 0) || (y > 0)) {
			y := y - 1;
		} else {
			--test exit
			exit;
		}
	continue;
	end;
}
