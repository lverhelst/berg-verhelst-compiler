int a;

int f(int x, bool b) {
	int y;

	y := readint();

	if (b) {
		int u;
		u := 2 * x;
		return u;
	}
	else {
		y := f(u, not b);
		return y;
	}

	return y;
}

int main(void) {
	int m;
	a := 1;
	m := f(a, false);
	writeint(m);
	return 1;
}
